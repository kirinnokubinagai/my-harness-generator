{
  "$schema": "https://json.schemastore.org/claude-code-hooks",
  "_comment": "Active only in harness projects (where .my-harness/.config exists in cwd or a parent directory). Exits immediately with code 0 in all other projects.",
  "UserPromptSubmit": [
    {
      "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/log-user-prompt.sh"
    }
  ],
  "Stop": [
    {
      "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/log-claude-output.sh"
    }
  ]
}
