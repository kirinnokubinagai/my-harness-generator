# direnv 設定: ディレクトリに cd した瞬間に flake.nix の dev shell へ自動で入る。
# 初回のみ `direnv allow` を実行する必要がある。
# これにより `nix develop` の打ち忘れによる impure 実行を防止する。
use flake
dotenv_if_exists .env.local
watch_file flake.nix
watch_file flake.lock
